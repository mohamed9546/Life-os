"""AI task handlers. Each task mirrors its TypeScript counterpart in
`src/lib/ai/tasks/` so the two implementations can run side-by-side via
the USE_PYTHON_AI feature flag and be compared for parity."""
